2) yes
3) Temporary AWS credentials generated by AWS for the attached IAM role.
    acceskeyid, secretAccessKey, Token

4) no, because i didn't gave the write permission to the Instance profile.

log ques:
    ec2 obtains the temp credentials from the IMDSv2.